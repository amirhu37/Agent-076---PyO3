// use pyo3::prelude::*;
// use rand::distributions::Uniform;
// use rl::{agent::Agent, env::Env};

// #[pymodule]
// fn society(py: Python, m: &PyModule) -> PyResult<()> {
//     m.add_class::<Env>()?;
//     m.add_class::<Agent>()?;

//     Ok(())
// }

fn main() {}

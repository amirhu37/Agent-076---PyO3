# mymodule.pyi

from typing import List, Optional, Any
import numpy as np

from tests.mymodule import IDValueError

# class IDValueError(Exception):
#     def __init__(self, msg: str) -> None: ...
    
class Agent:
    id: int
    name: str
    actions: np.ndarray | list[int]
    utility: float
    
    def __init__(self, name: str, actions: np.ndarray | list[int] = [], utility: float = 0.0,) -> None: 
        if isinstance(actions, np.ndarray):
            self.actions = np.array(actions)
        elif isinstance(actions, list):
            self.actions = list(actions)
        else :
            raise IDValueError("actions must be a list or a numpy array.")
        ...
    
    @property
    def get_actions(self) -> np.ndarray: ...
    